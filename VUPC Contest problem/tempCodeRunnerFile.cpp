int t = scan.nextInt();
// 		int count = 0;	// roll 갯수
// 		for(int tc=0; tc<t; tc++) {
// 			int x = scan.nextInt();
// 			/* 주사위 중 가장 큰 7로 최대한 점수 맞춘다. */
// 			count = x / 7;
// 			/* 나누어 떨어지지 않을경우 주사위 1번 더굴려야함. */
// 			count += (x%7 == 0) ? 0 : 1;